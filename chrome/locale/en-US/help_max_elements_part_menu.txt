The max element number in each part of the list.

With mode "1", it's better to set "Max nb line in each part" to 15-30.
With mode "2", it's better to set "Max nb line in each part" to 5-10.
With mode "3", it's better to set "Max nb line in each part" to 5-10.
